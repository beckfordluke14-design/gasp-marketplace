# ⚡ THE GASP SYNDICATE ARCHITECTURE (PRO-STRIKE V2.0)

This platform is a high-performance, secure AI Creator Syndicate. It utilizes a **Hybrid Asset Pipeline** to bypass Supabase storage limits and achieve a **78% LCP speed gain.**

---

## 🔑 1. THE FORTRESS: VPS ACCESS
The assets are served from a **Windows VPS** acting as the Syndicate High-Speed Vault.

*   **Public URL**: `https://asset.gasp.fun` (Routed via Cloudflare Grey Cloud)
*   **Direct IP**: `108.181.187.14`
*   **SSH Port**: `22` (Direct Strike) or `1097` (RDP)
*   **Username**: `Administrator`
*   **Master Password**: `blackforest1!`
*   **Root Folder**: `C:\gasp_assets`

---

## 📡 2. THE STACKS: HYBRID ROUTING
We use a **Triple-Tier Healer** logic to ensure 100% asset uptime.

1.  **Tier 1 (Strike Speed)**: `asset.gasp.fun` serves **Optimized WebP (Q95)** images generated by our conversion engine.
2.  **Tier 2 (Sovereignty)**: If WebP is missing, the VPS serves the original **RAW PNG or JPEG** from the local drive.
3.  **Tier 3 (Heritage Fallback)**: If the VPS is down or the file was never migrated, the system automatically redirects to **Supabase Storage** (`chat_media` bucket).

### 🛠️ Important Configuration:
-   **Caddy (Reverse Proxy)**: Configured with a **Grey Cloud SSL** cert to bypass TLS-ALPN challenges of the Windows Firewall.
-   **Headers**: All assets serve with `Cache-Control: public, max-age=31536000` to ensure edge speed.

---

## 🚀 3. DEPLOYMENT & WORKFLOW
-   **Frontend**: Hosted on **Railway** (Syncing with `main` branch).
-   **Rebuilds**: Any push to `main` triggers a global rebuild (Approx 2 mins).
-   **Database**: Supabase PostgreSQL.
-   **Branding**: Global GASP logo and OG images are stored in `/public`.

---

## 🔥 4. OPERATIONAL COMMANDS (VPS)
Run these "Strike Scripts" locally on the VPS for maintenance:
-   `py bulk_optimize.py`: Bulk converts existing images to WebP.
-   `py ultimate_healer.py`: Verifies every link in the DB and points it to the best tier.
-   `py repair_vaults.py`: Restores locked "Vault" images to PNG (High Fidelity).

---

## 🦾 AGENT REMINDER:
**Never hardcode API keys.** All secrets (Gemini, Grok, Supabase) are managed via **Next.js Environment Variables** to prevent GitHub security incidents.

**THE SYNDICATE IS ACTIVE.** 🚀💎🔐
